{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests",
    "schedule:daily"
  ],
  "timezone": "Asia/Singapore",
  "description": "platformAutomerge stays false: main has no required status checks, so GitHub-native automerge would merge before (or without) CI; Renovate-side automerge waits for every present check to pass.",
  "automerge": true,
  "platformAutomerge": false,
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update fixed-commit GitHub archives from their declared branches",
      "managerFilePatterns": ["/^\\.chezmoiexternal\\.toml$/"],
      "matchStrings": [
        "# renovate: currentValue=(?<currentValue>\\S+)\\s+url\\s*=\\s*\"https://github\\.com/(?<depName>[^/\\s]+/[^/\\s]+)/archive/(?<currentDigest>[a-f0-9]{40})\\.tar\\.gz\""
      ],
      "packageNameTemplate": "https://github.com/{{{depName}}}",
      "datasourceTemplate": "git-refs"
    },
    {
      "customType": "regex",
      "description": "Update standalone tools pinned in GitHub Actions workflows",
      "managerFilePatterns": ["/^\\.github\\/workflows\\/.*\\.ya?ml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+\\S+_VERSION:\\s*(?<currentValue>\\S+)"
      ]
    }
  ],
  "packageRules": [
    {
      "description": "Keep routine dependency updates from every manager in one PR",
      "matchPackageNames": ["*"],
      "groupName": "All dependencies",
      "groupSlug": "all-dependencies",
      "separateMajorMinor": false
    },
    {
      "description": "Keep this at least as long as the min-release-age the dev-tools installer writes to ~/.npmrc, which rejects anything younger outright.",
      "matchDatasources": ["npm"],
      "minimumReleaseAge": "1 day"
    },
    {
      "description": "uv only resolves PyPI artifacts older than the exclude-newer window in dot_config/uv/uv.toml, so a pin newer than that window cannot be installed at all. Keep this value at least as long as that window.",
      "matchDatasources": ["pypi"],
      "minimumReleaseAge": "3 days"
    },
    {
      "description": "TypeScript 7 ships no lib/tsserver.js, which typescript-language-server launches.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7"
    }
  ]
}
