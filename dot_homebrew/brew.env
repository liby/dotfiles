# Brewfile installs the apps that carry their own updater, and those updaters own later
# upgrades. Homebrew 7 otherwise reinstalls an `auto_updates true` cask whenever the app
# bundle is older than the tap, racing the app's updater and quitting the running app.
HOMEBREW_NO_UPGRADE_AUTO_UPDATES_CASKS=1
